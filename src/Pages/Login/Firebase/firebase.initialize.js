import { initializeApp } from "firebase/app";
import firebaseConfig from "./firebase..config";

const initializeFirebse = () => {
    initializeApp(firebaseConfig)
}

export default initializeFirebse;